{# templates/index.html.twig #} {% extends 'master.html.twig' %} {% block title
%}Welcome to GEEKS Shop! {% endblock %} {% block content %}

<section class="bg-white py-8">
  <div class="container mx-auto flex items-center flex-wrap pt-4 pb-12">
    <nav id="store" class="w-full z-30 top-0 px-6 py-1">
      <div
        class="
          w-full
          container
          mx-auto
          flex flex-wrap
          items-center
          justify-between
          mt-0
          px-2
          py-3
        "
      >
        <a
          class="
            uppercase
            tracking-wide
            no-underline
            hover:no-underline
            font-bold
            text-gray-800 text-xl
          "
          href="#"
        >
          Store
        </a>

      </div>
    </nav>

    {% for product in products %}
    <div class="w-full md:w-1/3 xl:w-1/4 p-6 flex flex-col">
      <a href="{{ path('product.detail', { id: product.id }) }}">
        <img
          src="{{ product.picture }}"
          alt="{{ product.name }} thumbnail image"
          class="
            shadow
            rounded
            w-full
            h-56
            align-middle
            border-none
            object-contain
          "
        />
        <div class="pt-3 flex items-center justify-between">
          <p class="">{{ product.name }}</p>
          {#
          <img
            src="{{ asset('images/icons/add-basket.png') }}"
            class="cart-icon"
          />
          #}
        </div>
        <p class="pt-1 text-gray-900">£{{ product.price }}</p>
      </a>
    </div>
    {% endfor %}
  </div>
</section>

{% endblock %}
